package misc;

/**
 * 1. We can store them and perform a XOR operation where all the duplicates get removed. But if
 * the stream can be very huge so XOR can handle big operations? Need to dig deeper into XOR
 * operations and how fast will it be.
 *
 * 2. Store it as a trie. This is be memory efficient as we do not occupy memory for every string
 * and searching will also be faster. It will take O(n). But again, deleting part has to be
 * looked deeply into.
 */
public class Problem001 {
}
