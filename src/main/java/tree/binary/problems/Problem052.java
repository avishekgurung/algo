package tree.binary.problems;

/**
 *
 * We can do a post order traversal (infact any traversal).
 * We replace the current node's value by the sum of left and right subtree but when we return
 * the value to the parent, we return sum of left + right + current node value.
 *
 */
public class Problem052 {
}
